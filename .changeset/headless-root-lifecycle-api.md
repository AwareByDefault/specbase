---
"@awarebydefault/specbase": major
---

Make the package root a headless library surface and add the versioned lifecycle snapshot API.

Programmatic CLI consumers must now import Commander exports from `@awarebydefault/specbase/cli`; the `specbase` and `openspec` binary entry points are unchanged.
