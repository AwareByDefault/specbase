# Tasks

## 1. Implement the capability
- [x] 1.1 Handle `--units` in `src/adapters/cli.ts`, reading `UNITS` from the domain
- [x] 1.2 Add a `bun test` case in `test/convert.test.ts` for `--units`

## 2. Activate enforcement
- [x] 2.1 Flip the `units-tests` binding from `planned` to `active` once the test passes
