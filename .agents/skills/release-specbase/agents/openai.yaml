interface:
  display_name: "Release Specbase"
  short_description: "Audit, prepare, publish, and finalize releases"
  default_prompt: "Use $release-specbase to audit the current release state and take the next safe release step."
