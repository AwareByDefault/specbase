# Short title

The command accepts one or more files or globs. Run it with `--json` to get a
clean aggregate, and pass `--max 8` to gate the exit code.

```python
# this forbidden sentence is code, not prose
def robust_thing():
    return "cutting-edge"  # marker words here do not count
```

Fenced code above contributes nothing. Neither does this inline `utilize`.
